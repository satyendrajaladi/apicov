The APICOV is very simple, easy to use tool written completely in Qt.
APICOV estimates the coverage of the APIS in the testcases comparing with the header files.
Most of the interface is in a single class.
The UI is seperated from the implementation of the logic.
